spring:
  datasource:
    url: jdbc:h2:mem:muhou;MODE=MySQL;DB_CLOSE_DELAY=-1;DATABASE_TO_LOWER=TRUE
    driver-class-name: org.h2.Driver
    username: sa
    password:
  flyway:
    enabled: false

muhou:
  order:
    timeout-auto-cancel-enabled: false
